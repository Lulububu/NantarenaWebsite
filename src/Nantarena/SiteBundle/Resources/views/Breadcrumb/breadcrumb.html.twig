<ul class="breadcrumb">
    {% for item in breadcrumb %}
        {% if loop.last %}
            <li class="active">{{ item.name }}</li>
        {% else %}
            <li><a href="{{ item.path }}">{{ item.name }}</a> <span class="divider">/</span></li>
        {% endif %}
    {% endfor %}
</ul>